<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppTryLine" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="estilo_mensaje" parent="">
        <item name="android:background">@drawable/mensaje</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="color_texto_predeterminado" parent="">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="iconos_perfil" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="tint">@color/black</item>
        <item name="android:background">@color/transparente</item>
    </style>
    <style name="color_gris" parent="">
        <item name="android:background">@color/gris_flojo</item>
    </style>
    <style name="barra_mensaje" parent="">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="edittext" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <!-- Primary brand color. -->
        <item name="boxStrokeColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="botones_continuar" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="android:tint">@color/gris_flojo</item>
    </style>
    <style name="Theme.AppTryLine" parent="Base.Theme.AppTryLine" />
</resources>